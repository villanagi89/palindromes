var palindrome = {};

function palidrome(string){
  // check to see if something is reversed.
  return string.split('').reversed;

//check if there are two palindrome
  function count(arr){
    for (var i=0; i < arr.length; i++){
      palindrome.push(arr[i] ==reverse(arr[i]));
    }
    return palindrome;
}
